# GitHub Pages 설정
# Jekyll 처리 비활성화

# 모든 파일을 있는 그대로 서빙
include:
  - _headers
  - .htaccess
  - .nojekyll

# 제외할 파일들
exclude:
  - README.md
  - .git
  - .github

# Markdown 엔진 비활성화 (순수 HTML만 사용)
markdown: kramdown
kramdown:
  input: GFM
  hard_wrap: false

# 플러그인 없음
plugins: []

# 기본 설정
safe: true
